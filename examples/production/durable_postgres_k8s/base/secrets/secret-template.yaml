# PLACEHOLDER Secret. Operators MUST replace before any non-local apply.
# - dev overlay: kustomize secretGenerator from local file (gitignored)
# - prod overlay: SealedSecret (see components/sealed-secrets/) — bitnami controller required.
# See secrets/README.md for the rotation runbook.
apiVersion: v1
kind: Secret
metadata:
  name: durable-daemon-secrets
type: Opaque
stringData:
  # Replace these placeholders. DO NOT commit real values.
  postgres_password: REPLACE_ME_POSTGRES_PASSWORD
  fernet_key: REPLACE_ME_FERNET_KEY_BASE64
