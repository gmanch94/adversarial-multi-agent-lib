apiVersion: v1
kind: ServiceAccount
metadata:
  name: durable-daemon
# Pod sets automountServiceAccountToken: false; this SA exists only for
# audit / RBAC binding hooks. Daemon does not call the k8s API.
automountServiceAccountToken: false
