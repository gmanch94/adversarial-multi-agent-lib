apiVersion: v1
kind: Service
metadata:
  name: durable-daemon
  labels:
    app.kubernetes.io/name: durable-daemon
    app.kubernetes.io/component: daemon
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: durable-daemon
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
