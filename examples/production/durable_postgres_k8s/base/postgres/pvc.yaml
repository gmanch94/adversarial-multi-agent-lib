apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 10Gi
  # storageClassName intentionally unset; cluster default applies.
  # Overlay (prod) may patch storageClassName explicitly.
