# NEXT_SESSION.md

2026-07-27 — HEAD `6c315c3` on `main` (CGT-audit **L-6** ledger claim-text-cap wiring + warn-on-drop, D-A11-10) + this `[skip ci]` doc-sync. Prior: M-2/M-3 example-workspace fix `9d0fbd6` (D-A11-9 + G10); toolchain pin `04d1769` (CI GREEN 3.11/3.12/3.13); G8 guard `70258a5`. Tree clean, in sync with origin. Deep pre-2026-07-25 history in `git log` / `decisions.md` / `LESSONS_LEARNED.md` / `docs/security-audits/`.

**Scale:** 7 domains · 71 workflows (30 veto) · 2348 lib + 207 sibling tests · 288 templates · ruff+mypy clean (120 src). research 4+assurance · parole 1 · retail 8 · pc 7 · industrial 8 · healthcare 8 · lifesciences 35. Durable subpackage + 5 prod siblings.

- **L-6 LANDED (D-A11-10, `6c315c3`).** `BaseWorkflow.__init__` now builds the default ledger `ClaimLedger(config.ledger_path, max_claim_chars=config.max_claim_text_chars)` — previously unwired, so the ledger kept its own 2000 default while `_register_claims` truncated to the Config cap; raising `max_claim_text_chars` above 2000 made `ClaimLedger.add` raise and the `except ValueError` swallowed it → silent audit-trail gap. Both halves shipped: wire the cap (default path can no longer raise) + the residual swallow now `warnings.warn`s (length-only message, PHI-safe — rests on `_bound`'s content-free f-string). `workflow.py:51` is the ONLY `ClaimLedger(` site in `src/`, so durable resume inherits it. 2 negative tests in `test_workflow_register_claims.py::TestRegisterClaimsLengthBound` (first is RED pre-fix); reproduced live before the fix. The test IS the regression guard (single spine site, not a convention → no new CI gate). Gate: ruff 0.15.12 · mypy 120 · **2348 tests**.
- **L-2 DOWNGRADED (not fixed).** The "4.8 MB in metadata" claim is already false — metadata dedups every flag list at read (`dict.fromkeys`), and the accumulator is never re-injected into a prompt. Only residual = *intermittent* checklist `len()` inflation on exact-string flag repeats across rounds; a 59-file convention sweep (+ helper + guard + 59 test re-verifies) is not justified for a sometimes-off count already deduped where it matters. Revisit only if a real run shows an inflated count (fix then = dedup-on-write, metadata output unchanged). See audit resolutions.

## Last landed — M-2/M-3 example-workspace fix (D-A11-9, this session, PUSHED `9d0fbd6`)
- **44 determination examples** (35 lifesciences + 8 healthcare + 1 parole) now set `workspace_dir=tempfile.mkdtemp(prefix="<name>-")` — a per-run 0700 dir. `context_for_round` is round-scoped, NOT run-scoped, and the base workflow builds `ResearchWiki` under `workspace_dir`; a shared workspace replayed the prior case's FEEDBACK critiques into the next run's executor prompt (cross-case bleed, PHI for donor/healthcare/parole — M-2). A fixed world-writable `/tmp/<name>` was also a symlink-preplant target (M-3). The 2 `WORKSPACE_DIR`-env examples keep the override and read ledger/wiki from the resolved `config.workspace_dir`.
- Guard **G10** ([test_workflow_conventions.py](../tests/unit/test_workflow_conventions.py), both prongs mutation-tested): no fixed-literal `workspace_dir` (prong 2, M-2) + no `/tmp/` text (prong 1, M-3); `mkdtemp` / a variable / an env override pass; `clinical_trial_durable` allowlisted (checkpoint resume needs a stable dir). `ResearchWiki` / `context_for_round` got a scope-note docstring (no behaviour change).
- Independent pre-commit review caught the 4 healthcare `.healthcare_workspace` + parole cwd cases the `/tmp` sweep missed (the sweep + guard keyed on the `/tmp/` *spelling*, not the shared-persistent-wiki *precondition*); advisor caught a parole mis-clearance (its workflow does read the wiki). Autonomy-default: caller-layer per-run workspace over spine run-scoping (the wiki is a documented cross-run shared store). Gate: ruff · mypy 120 · **2346 tests**.

## Last landed — L-1 / G8 gate-guard (D-A11-8)
- **G8** in [test_workflow_conventions.py](../tests/unit/test_workflow_conventions.py) closes CGT ship-audit **L-1**: the D-A11-1 convergence-gate invariant now has a recomputing static guard (G5/G6 covered only its neighbours). Three prongs, all mutation-tested — **G8a** flag-gated module calls `_flag_classes_unresolved` with LIVE args (headers arg is the `_FLAG_HEADERS` Name, flags arg reads the flag values; a hollowed `_flag_classes_unresolved(critique, (), ())` is fail-OPEN, same drift class G2 catches for `extract_flags`); **G8b** no bare `not any(<flag values>)` in `run` (the legit short-circuit is the sibling `_format_flag_section`, never reached by a run-scoped walk); **G8c** non-gated workflows positively asserted to have grown no flag gate (A11-M6.2 — no silent skip).
- **No code change** — census: 72 workflow files, 67 flag-gated, all 67 already route through the helper; 5 non-gated (research base/assurance/rebuttal/idea + the durable subclass = the 72-vs-71 delta). G8 is the missing check, not a fix.
- Mutation harness (5 targeted mutations) confirmed each prong fires only on its bug shape with **no G1 collateral** — the RED is genuinely G8's (2026-07-23 mutation-lands lesson honoured; harness restored the tree via git after a `Path.write_text` LF→CRLF flip on one file). Gate: ruff clean · mypy 120 src · **2275 tests** (as of that commit). Advisor consulted on design (arg-check fold-in was theirs); independent reviewer skipped per advisor (test-only diff, mutation-testing is the real check).

## Resolved — CI green (toolchain pinned, `04d1769`)
- Was RED since 2026-07-25: last code push `c768994` (D-A11-7) failed at the ruff step (`I001` · `RUF100` unused `# noqa: E402` · `S110` in `examples/production/_shared/tenant_env.py` + siblings), hidden by three `[skip ci]` docs commits; the 3.11–3.13 matrix had not run since 2026-07-23. Root cause: ruff + mypy were unpinned ranges, so CI installed drifting versions while local passed clean.
- **Fixed:** pinned `ruff==0.15.12` and `mypy==1.20.2` (the locally-verified-good versions) in `pyproject.toml` `[dev]` — the two fail-hard gate tools. `mypy python_version` is already `3.11` in config, so the exact pin reproduces local on any runner. Run `30299175609` GREEN on all three Python versions — first full matrix pass since 2026-07-23, which also confirmed G8 + all D-A11-7 work pass 3.11/3.12/3.13.
- Residual (non-blocking): `pytest` / `pytest-asyncio` left as ranges (test runners, not the false-gate-failure class). If they ever drift, pin them too. Don't reintroduce open-range pins on ruff/mypy — that is what caused this.

## Last landed — M-1 flag-header prose sweep (D-A11-7) + arch-doc refresh (all pushed)
- Colon-suffixed flag headers retired from criteria PROSE repo-wide (44 flag-gated workflows + 36 `*_review.md` templates; 100 exact-case + 4 retail lowercase-slash-format misses). Colon-form now lives ONLY in the emission block, so no echoed rubric can shadow real findings. Cross-refs `Flag under X FLAGS.`; scoring `zero X FLAGS, then ready`. Bytes-level codemod preserved retail CRLF.
- Guard **G9** ([test_workflow_conventions.py](../tests/unit/test_workflow_conventions.py), modules + templates, all prongs mutation-tested): 2 prongs matching the two runtime matchers — generic case-sensitive `[A-Z…] FLAGS:` (non-tuple, fail-OPEN) + declared case-insensitive (`_header_anchor_re` is `(?mi)`; caught 4 live retail misses). Independent review SHIP-WITH-FOLDIN.
- **VETO follow-up LANDED (user-confirmed, 2nd commit `c768994`):** `REVIEWER VETO:` marker swept from prose across all 53 veto files (30 workflows + 23 templates, 106 occurrences: `REVIEWER VETO: line` → `REVIEWER VETO line`, `"REVIEWER VETO: None"` → `"None"`). Emission `REVIEWER VETO: <verbatim…>` line + `extract_veto_directive` code marker unchanged. G9 gained a `REVIEWER VETO:` prong (mutation-tested). The whole FLAGS+VETO section-header-echo class is now retired from criteria prose.
- **VERIFY ON FIRST LIVE VETO RUN (not testable locally):** the veto-instruction wording changed; the test suite uses canned critiques, so none exercises whether a REAL reviewer still emits a parseable `REVIEWER VETO:` line from the reworded criteria. The emission block still pins the exact output format, so it should — confirm `metadata['veto_reason']` populates on the first live veto (advisor flag; post-deploy-probe mindset). The veto-directive FORMAT NOTE's `Write all continuation lines in free prose` catch-all already covers the uppercase `FLAGS:`/`VETO:` stop markers (its enumeration is illustrative, not exhaustive), so the note needs no change.

## Last landed — CGT ship-audit spine fixes (pushed)
- **H-1 / D-A11-6** (`e4463ca`): retired per-call-site `sanitize_for_prompt(request.to_prompt_text(), max_chars=6000)` (< n_fields×1500 → dropped trailing fields off executor prompt → evidence left the loop). Now shared `sanitize_request_text` in `core/_internal.py` (control-char/NFC + `_MAX_REQUEST_PROMPT_CHARS=60_000` backstop) at all 67 call sites. Guard **G7** + regression. Report: [audit §H-1](security-audits/2026-07-25-cgt-ship-audit.md).
- **C-1 / D-A11-5** (`fca3c5a`): parser unions every header occurrence / `extract_veto_directive` first-non-empty.

## Backlog (none blocking)
- **CGT-audit backlog:** L-1 (G8) · M-2/M-3 (D-A11-9) · **L-6 (D-A11-10)** all LANDED; **L-2 DOWNGRADED** (self-mitigated). **Remaining backlog: L-3, L-4, L-5, L-7, L-8** (all LOW) — see audit table + resolutions. L-3 (prompt says 7.5, code enforces 8.0 across 14 no-veto lifesciences modules; fail-closed doc-drift) is the next cheap-ish one; L-7 (no non-empty `*Request` validation) is the largest (convention-level across 71 workflows).
- **Residual — M-2 class, non-PHI, G10-invisible:** 23 industrial/pc/retail examples set NO `workspace_dir` → inherit `Config` default `"."` → a persistent cwd `wiki.json` shared across runs (same cross-case bleed, business data not PHI). Fix = per-run `mkdtemp` like the 44, OR make the library default isolated (spine change). Not a per-example defect; deferred.
- Longer: PyPI publish; 19 industrial + 19 healthcare + 4 CGT Phase-2 designs locked-not-built (D-LIFESCI-9+); L-HEALTH-4.

## NOT to do
- Don't reintroduce a fixed-literal `workspace_dir` in an example (G10 prong 2) or any `/tmp/` path (prong 1); use `tempfile.mkdtemp` or a `WORKSPACE_DIR` env override. Don't add to `_FIXED_WORKSPACE_ALLOWLIST` without a stated persistence reason — only `clinical_trial_durable` (checkpoint resume) qualifies today.
- Don't reintroduce the `sanitize_for_prompt(to_prompt_text(), max_chars=N)` cap — route via `sanitize_request_text` (G7; H-1). Per-field markers = L-IND-5, known gap.
- Don't un-wire the default ledger at `workflow.py:51` — it MUST pass `max_claim_chars=config.max_claim_text_chars` (D-A11-10/L-6); dropping it re-opens the silent-claim-drop when an operator raises the Config cap above 2000. `test_workflow_register_claims.py::TestRegisterClaimsLengthBound` is the guard. Keep the `_register_claims` drop-warning length-only (no claim text → PHI-safe).
- Don't reintroduce parser occurrence-selection (C-1); don't loosen `_is_section_header`/`_is_sibling_header_lhs`; don't re-add `cap_field`.
- Don't restate a `<HEADER> FLAGS:` (with colon) in criteria PROSE — colon-form lives only in the emission block; refer to sections colon-free (D-A11-7/M-1, G9 enforces both prongs).
- Don't restore bare `not any(...)` gates OR hollow the helper call to `_flag_classes_unresolved(critique, (), ())` — use it with live `_FLAG_HEADERS` / flag-dict args (G8 enforces D-A11-1, all three prongs).
- Don't reuse D-LIFESCI-1..8 (next = 9+); no domain base class; block-form `inputs:` is supported; no brand names (D-LIFESCI-3).
- No digit-containing flag HEADERs (361/351/1271 prose-only, H-IND-1); no GM/vector product as a 361-candidate test.
- Don't add durable to system-context diagram unless asked.

Sources: [decisions.md](decisions.md) · [LESSONS_LEARNED.md](LESSONS_LEARNED.md) · [gaps](production-readiness-gaps.md).
