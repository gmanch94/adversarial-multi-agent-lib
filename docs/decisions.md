# decisions.md

Append-only. Each row is locked once added. Revisit by adding a new row, not editing.

| # | Date | Decision | Rationale | Alternatives rejected |
|---|------|----------|-----------|----------------------|
| 1 | 2026-05-12 | Executor: `claude-opus-4-7` with `thinking: {type: "adaptive"}` | Highest-capability model with adaptive thinking; xhigh effort for agentic tasks | claude-sonnet-4-6: lower cost but reduced reasoning depth for long-horizon research |
| 2 | 2026-05-12 | Reviewer: GPT-4o (OpenAI) by default | Cross-model family pairing prevents echo-chamber effect per ARIS §3.2 | Second Claude model: same-family reasoning shortcuts could invalidate adversarial critique |
| 3 | 2026-05-12 | Python 3.11+ with async throughout | Anthropic SDK is async-native; ML research tooling is Python-first | TypeScript: viable but less natural for research pipelines |
| 4 | 2026-05-12 | JSON-based persistence for ledger + wiki | Zero-infra, portable, human-readable; appropriate for research template scale | SQLite: overkill for template; Postgres: requires infra |
| 5 | 2026-05-12 | Markdown-based skill registry (YAML frontmatter) | Mirrors ARIS §3.1 design; skills are readable/editable without code changes | Python decorators: harder to share and version outside codebase |
| 6 | 2026-05-12 | Dataclasses for internal objects, Pydantic at API boundaries | Dataclasses: lightweight, no dependency for value objects; Pydantic: validation where it matters | All-Pydantic: heavier; all-dataclasses: no validation at boundaries |
| 7 | 2026-05-12 | Convergence: score >= threshold OR max_rounds | Dual criterion per ARIS §4.2; score-only could loop forever if reviewer is strict | Max-rounds-only: never converges on quality; score-only: unbounded cost |
| 8 | 2026-05-12 | Multi-provider executor: thin facade pattern; Anthropic + Gemini in scope; AWS Bedrock deferred | Thin facade mirrors existing ReviewerAgent pattern; Gemini 2.5 Pro selected (user has keys, free tier available); Bedrock deferred — no free tier for Claude models, no concrete user need yet. Same-family pairing (executor + reviewer both Anthropic) emits UserWarning per D2 echo-chamber risk. EffortLevel maps to Gemini thinking_budget: low→0, medium→4096, high→8192, xhigh→16384 | Capability-reporting protocol: heavier, no existing pattern in codebase. Lowest-common-denominator BaseAgent: loses Anthropic-specific features (adaptive thinking, streaming). Bedrock: pay-per-token from first call, no free tier for Claude. |
| 9 | 2026-05-13 | Retail domain mirrors parole/ structure exactly: per-workflow `*Request` dataclass + domain-specific FLAGS convergence gate. Demand uses `ASSUMPTION FLAGS` (executor must ground or remove flagged adjustments); labor uses `COMPLIANCE FLAGS` (executor must fix flagged labor-law violations). Skill templates flat with `demand_*` / `labor_*` prefixes. Synthetic data only — no live Kroger API. | Pattern parity with parole minimises new abstractions and keeps the domain-extension recipe explicit. Domain-specific FLAGS string makes the convergence gate self-documenting and reuses the parole bias-flag extraction shape. Flat skill dir avoids nested registry resolution; prefixes preserve readability. | Shared `RetailWorkflow` base class: premature — two workflows differ enough that shared layer would be thin. Single workflow file with two classes: violates single-responsibility, hurts readability. Live retail data integration: out of scope for teaching template — `PRODUCTION_GAPS` documented in each module docstring. |
| D-RETAIL-1 | 2026-05-13 | Reviewer-veto pattern (new): a workflow may use `REVIEWER VETO:` as a halt directive independent of score and flag counts. First used by `RecallScopeWorkflow`. Convergence requires `approved AND no flags AND no veto`. On veto: workflow halts immediately, writes the wiki feedback + ledger claims for the vetoed round (audit-trail-first), captures veto verbatim in `metadata['veto_reason']`, sets `metadata['vetoed']=True`, prepends a `⚠️ REVIEWER VETO` banner to output. Veto check runs AFTER flag extraction so all flags from the vetoed round are also captured. | Recall scoping is an irreversible-class decision (ARIS §3.2 cross-model audit applies). A reviewer that detects a life-safety gap must be able to halt the loop regardless of score — score reflects quality, not safety. Audit-trail-first ordering ensures regulators can review what was vetoed and why. | (a) Use score=0 to signal veto: conflates quality with safety, lossy; (b) Replace draft output with a synthetic "VETOED" message: hides the draft from the safety officer who must judge whether to override; (c) Skip ledger/wiki writes on veto: makes audit trail incomplete for regulators. |
| D-RETAIL-2 | 2026-05-13 | No shared base class for retail workflows during the six-scenario sweep. Each workflow inlines `_extract_flags`, `_register_claims`, `_build_*_checklist`. Re-evaluate helper extraction after PR #2-3 of the sweep land. | Premature abstraction is a CLAUDE.md-listed anti-pattern. With only 2 workflows in hand, no evidence of duplication-without-divergence. After 3 workflows exist, if parsers genuinely differ only in section-header string, extract `_extract_flags(critique, header)` into `core/_internal.py`. | Build `FlagGatedWorkflow` base now: forces every later workflow into one shape before we know whether the shape fits; harder to evolve once subclassed. |
| D-RETAIL-3 | 2026-05-13 | Skill-template prefix per scenario noun: `recall_*`, `loyalty_*`, `promo_*`, `supplier_*`, `replenishment_*`, `private_label_*`. Continues the `demand_*` / `labor_*` precedent from D9. | Avoids collisions, easy to grep, keeps `bundled_skills_path("retail")` a single flat directory. | Subdirectories per scenario: harder to load with the current `SkillRegistry`; doesn't add value at this scale. |
| D-RETAIL-4 | 2026-05-13 | One example file per scenario at `examples/retail/<scenario>.py`, mirroring `demand_forecasting.py` + `labor_scheduling.py`. Each example uses synthetic data and prints the safety/buyer/manager checklist. | Discoverability: `examples/retail/` is the entry point a new user opens. One-to-one with workflows. | Single mega-example: too long, hides the per-scenario request shape. |
| D-RETAIL-5 | 2026-05-13 | Synthetic data only for retail. PRODUCTION_GAPS in each module docstring names the live integrations (POS, HCM, GS1 EPCIS, FDA / USDA feeds, etc.) that production deployment would require. | Teaching template; live retail integrations are out of scope and would require enterprise contracts. PRODUCTION_GAPS makes the gap explicit so a downstream consumer can scope it. | Half-mocked integrations (e.g. fake POS adapter): risks being mistaken for production-ready. |
| D-RETAIL-6 | 2026-05-13 | Per-workflow test suite must cover: convergence on clean input; non-convergence when flags present; flag parser extraction under each header; claims registered into ledger; disclaimer present in output. Workflows that use the veto pattern additionally cover: veto with high score halts (score does not override); veto records flags from vetoed round; absence-of-veto path unchanged. | Locks the convention; prevents regression as the convention is copied across six PRs. | Looser convention (just convergence + non-convergence): misses regressions in flag parsing and audit-trail writes, which are the bug-prone parts. |
| D-PC-1 | 2026-05-14 | New domain: specialized B2B Property & Casualty insurance under `src/adv_multi_agent/pc/`. Scope is commercial lines only (claims-handling + complex underwriting + cyber). Personal lines (auto/home/health/life) explicitly out of scope. Synthetic data only — no live Guidewire / Origami / NAIC / ISO feeds. Build sequence anchored on Claims Reserve Estimation; subsequent PRs (Coverage/Bad-Faith, Commercial Underwriting, Cyber) sequenced after anchor sign-off. Design doc: [`docs/superpowers/specs/2026-05-14-pc-domain-design.md`](superpowers/specs/2026-05-14-pc-domain-design.md). | P&C concentrates the three properties ARIS targets: irreversibility (bound policy / set reserve), regulator audit (NAIC, SOX, state DOI, Schedule P), asymmetric information (insured vs underwriter; claimant vs adjuster). Echo-chamber risk is concrete — precedent-bias in claims/underwriting is documented in industry literature. Commercial-only narrows the surface to the highest-value decisions and avoids the regulated personal-lines rate-filing minefield. | (a) Personal lines: heavier regulatory filing surface (Form RF-1, state-specific rate caps) overshadows the adversarial-AI value; (b) Reinsurance treaty placement as anchor: heavy actuarial dependency makes synthetic-data examples weak; (c) Live integration: enterprise contracts (Guidewire ClaimCenter, Origami) out of scope for a teaching template. |
| D-PC-2 | 2026-05-14 | Anchor scenario: `ClaimsReserveWorkflow` with reviewer-veto + triple-flag gate (RESERVE / PRECEDENT / LITIGATION). Mirrors `RecallScopeWorkflow` structure exactly. First PR establishes the P&C-namespace conventions (skill prefix, example shape, test shape). | Reserve estimation is the canonical SOX-auditable irreversible decision in P&C — under-reserving has documented restatement consequences (e.g. AIG 2005, MBIA 2008). The veto+triple-flag shape is already validated in retail. Anchoring on the most-expressive shape forces the namespace conventions to be set correctly the first time, not retrofitted. | (a) Anchor on Commercial Underwriting: simpler (no veto) but doesn't exercise the veto path; (b) Anchor on Cyber: emerging-risk angle is attractive but synthetic control-attestation data risks being more illustrative than convincing; (c) Anchor on Coverage/Bad-Faith: best regulator-audit fit but requires verbatim policy wording + case-law strings that read as fabricated in synthetic form. |
| D-PC-3 | 2026-05-14 | P&C namespace mirrors retail exactly: `src/adv_multi_agent/pc/workflows/<scenario>.py`, `src/adv_multi_agent/pc/skills/templates/<prefix>_*.md`, `examples/pc/<scenario>.py`. Skill prefix per scenario noun: `reserve_*`, `coverage_*`, `underwriting_*`, `cyber_*`. No `PCWorkflow` base class (D-RETAIL-7 reasoning applies — base-class extraction stays rejected until cross-cutting concern appears across both domains). | Pattern parity across domains keeps the extension recipe explicit: a new domain is a sibling of `retail/`, not a special case. Skill-prefix scheme continues the D-RETAIL-3 precedent. Flat skill dir per domain avoids nested registry resolution. | Single `domains/` parent dir containing both `retail` and `pc`: refactor cost without benefit; current `retail` import path stable. Cross-domain base class: speculative — re-evaluate per D-RETAIL-7 cadence. |
| D-PC-4 | 2026-05-14 | Reviewer-veto pattern (D-RETAIL-1) reused for any P&C workflow with regulator-audit + irreversibility properties. Initial veto-using workflows: `ClaimsReserveWorkflow` (under-reserving → SOX restatement), `CoverageDecisionWorkflow` (bad-faith exposure on proposed denial). `CommercialUnderwritingWorkflow` and `CyberUnderwritingWorkflow` use triple-flag without veto — bind decisions are reversible at renewal in most cases. | Veto is the right tool when the cost of waiting one more loop iteration to halt exceeds the cost of a false-positive halt. Reserve adequacy + bad-faith denial both pass that test (SOX restatement / punitive-damages exposure). Underwriting binds do not — renewal cycle gives a natural correction window. | (a) Veto everywhere in P&C: false-positive halts on routine triple-flag scenarios add operator friction without proportional safety value; (b) No veto in P&C: loses the strongest argument for adversarial-AI in claims handling. |
| D-PC-6 | 2026-05-14 | **Specialty Lines track** added to the P&C domain. The four original scenarios (Claims Reserve, Coverage / Bad-Faith, Commercial Underwriting, Cyber) are recharacterised as the **Foundational** track — mainstream commercial. The Specialty track adds three workflows that match the actual scope of specialized B2B P&C: `EnvironmentalImpairmentWorkflow` (veto + KNOWN-CONDITION / TAIL / REGULATORY-OVERLAP), `ParametricCropWorkflow` (PERIL-MATCH / BASIS / ATTACHMENT, no veto), `GigPlatformLiabilityWorkflow` (veto + CLASSIFICATION / COVERAGE-GAP / REGULATORY-PATCHWORK). Group captive allocation and equine mortality are noted in the design doc as deferred specialty candidates. Same namespace as Foundational (`adv_multi_agent.pc.workflows`); skill-prefix convention extended (`environmental_*`, `crop_*`, `gig_*`). Design doc: [`docs/superpowers/specs/2026-05-14-pc-domain-design.md`](superpowers/specs/2026-05-14-pc-domain-design.md). | The original four were mainstream commercial; user feedback noted that "specialized" B2B P&C in industry usage means the *niche* commercial markets — agricultural, equine, environmental, group captives, gig-economy. These specialty markets concentrate three properties exactly: heterogeneous risk (no two captive members or two farms or two contaminated sites are alike), regulator-overlap (USDA-RMA, EPA/CERCLA, state TNC / classification statutes, NAIC RRG Act), and asymmetric information (insured-vs-underwriter on site history, classification posture, on-farm conditions). The cross-model reviewer pairing adds the most value precisely where same-family LLM precedent-bias is most likely. Environmental + Parametric Crop + Gig are the three with the most differentiated gate shapes (veto + no-veto + veto with retroactive-exposure as veto class), so they exercise distinct convergence-gate idioms. | (a) Skip specialty entirely: leaves the P&C domain mis-named — "specialized" without the specialty markets is misleading; (b) Build all five specialty markets (add equine + captive in the same batch): low-priority for token budget; defer until a concrete user need emerges; (c) Refactor Foundational + Specialty into separate sub-packages (`pc/foundational/` and `pc/specialty/`): premature — D-RETAIL-7 reasoning applies (avoid sub-package abstraction until cross-track concern emerges). |
| D-PC-5 | 2026-05-14 | Test convention for P&C mirrors D-RETAIL-6 verbatim: convergence on clean input; non-convergence per flag-class; per-header parser extraction; ledger-claim registration; disclaimer present; for veto-using workflows, veto-with-high-score halts and veto records flags from vetoed round. PRODUCTION_GAPS docstring names the live integrations a deployment would require (Guidewire ClaimCenter / PolicyCenter, Origami, AS400 mainframe extracts, NAIC Schedule P feed, ISO/Verisk loss-cost tables, state DOI filing portals). | Test convention is the cheapest forcing function against regression as the convention is copied across 4+ PRs. PRODUCTION_GAPS makes the synthetic-data → production gap explicit so a downstream consumer can scope it. | Looser test convention: misses regressions in flag parsing and audit-trail writes (same lesson as D-RETAIL-6). Half-mocked integration stubs: risks being mistaken for production-ready (same lesson as D-RETAIL-5). |
| D-IND-1 | 2026-05-14 | **New domain: Industrial Manufacturing & IoT** under `src/adv_multi_agent/industrial/`. Modelled on the Crown Equipment Corporation surface (vertically-integrated OEM with InfoLink telematics, DualMode automation, extended-lifecycle aftermarket). 27-workflow catalog organised into 6 tracks: Manufacturing Ops, Safety / Recall / Reserve, Strategic Capital, Industrial IoT, Automation Deployment, Service & Aftermarket. **MVP cut: 8 workflows** — `MakeVsBuyWorkflow`, `SupplierQualificationWorkflow`, `EngineeringChangeOrderWorkflow`, `QualityIncidentRootCauseWorkflow`, `ProductLiabilityRootCauseWorkflow` (veto), `RecallScopeManufacturingWorkflow` (veto, reuses `retail.recall_scope` shape), `SupplyChainResilienceWorkflow`, `TelematicsAnomalyTriageWorkflow`. **Phase 2 deferred** (19 workflows): all of Track 3 except #15, all of Track 4 except #17, all of Track 5, all of Track 6, plus #3/#4/#7 in Track 1 and #10/#11/#12 in Track 2. Personal electronics, batch chemicals, FDA medical devices, aerospace primes explicitly out of scope. Same namespace + skill-prefix convention as retail/pc (`makebuy_*`, `supplier_*`, `eco_*`, `quality_*`, `prodliab_*`, `recall_*`, `supply_*`, `telematics_*`). Synthetic data only — PRODUCTION_GAPS docstring per workflow. No `IndustrialWorkflow` base class (D-RETAIL-7 reasoning applies). Design doc: [`docs/superpowers/specs/2026-05-14-industrial-domain-design.md`](superpowers/specs/2026-05-14-industrial-domain-design.md). | Industrial concentrates the four ARIS properties: irreversibility (capex commit, recall scope, functional-safety re-cert, supplier disqualification, field-affecting ECO), regulator audit (OSHA 1904, CPSC § 15(b), ANSI/ITSDF B56.x, ISO 3691-4, EPA, SEC warranty-reserve disclosure), echo-chamber risk (OEM "we always do it this way" precedent-bias replicates in same-family LLM), asymmetric info (supplier stress / customer duty-cycle abuse / field-failure-mode patterns surface in different parts of the org). MVP-8 cut anchors on the highest-criticality + reuse-ready workflows: `RecallScopeManufacturing` is near-direct reuse of `retail.recall_scope`; `ProductLiabilityRootCause` bridges to `pc.ClaimsReserve`. The 2-of-8 veto count (vs P&C's 4-of-7) reflects that most industrial decisions are reversible until they reach the safety / recall / liability frontier. | (a) Build all 27 workflows: token-budget infeasible in one sprint, and Phase 2 set is design-locked so deferral has no design cost; (b) Anchor on `FunctionalSafetyCaseWorkflow` (#10): cert work is months of human-SME loop, weaker advisory ROI for the anchor; (c) Skip the IoT track for MVP: would mis-represent Crown's "hardware-enabled software platform" thesis and leave the InfoLink decision surface unaddressed; (d) Name the package `manufacturing/`: would mis-file the IoT / automation / service workflows. |
| D-RETAIL-7 | 2026-05-14 | Re-evaluation of D-RETAIL-2 with 8 retail workflows in tree (demand, labor, recall, loyalty, promo, supplier, inventory, private_label). **Outcome: keep inline; no `FlagGatedWorkflow` / `RetailWorkflow` base class.** The PR #16 helper extraction (`extract_flags`, `_register_claims`) was the correct unit of sharing; the per-workflow scaffolding above it stays inline. Surveyed divergences across the 6 triple-flag workflows that would force config-dict injection into a base class: (a) per-flag-header banner text + emoji + parenthetical directive; (b) metadata key names; (c) per-scenario checklist text; (d) reviewer-veto presence (recall only); (e) single-flag-class structure (demand, labor — already not sharing the triple-flag scaffold); (f) request-dataclass field shape per scenario. Five distinct injection points × six workflows = configuration surface that costs more than the inline duplication saves. | Helper-level sharing (already done in PR #16) captures the genuinely-duplicated logic. Inline code keeps each workflow file independently readable, makes per-scenario divergences visible at the call site, and avoids the "every new scenario must update the base class config schema" tax. Re-evaluation triggered by reaching the originally-stated "after 3 workflows" threshold (now well past). | (a) `FlagGatedWorkflow` ABC with `_FLAG_HEADERS: ClassVar[list[str]]` + abstract `_format_flag_section`: would replace 6 small files with 6 small files + 1 base, net LOC ~ flat but adds an inheritance hop on every read; (b) Strategy pattern with injected `FlagConfig` dataclass: pushes the divergence into a config object that is itself per-scenario, no real sharing. Defer the next re-eval until a 9th retail scenario lands OR a cross-cutting concern (e.g. instrumentation, async cancellation) lands that genuinely belongs above the workflows. |
| D-K8S-1 | 2026-05-18 | **Tier 1.2: kustomize, not Helm**, for the new `examples/production/durable_postgres_k8s/` sibling. Three overlays (`dev` / `staging` / `prod`) + opt-in `components/otel` + opt-in `components/sealed-secrets`. Library + `pyproject.toml` unchanged. | Kustomize ships with `kubectl` (no install step), produces plain YAML diffable in PRs, mirrors compose-stack hardening one-to-one without templating overhead. Operators who prefer Helm fork later. | (a) Helm: chart management + values templating not justified for a reference deploy; (b) Raw YAML without overlay: would require copy-paste between dev/staging/prod; (c) Jsonnet / cdk8s: extra language burden for a teaching template. |
| D-K8S-2 | 2026-05-18 | **Overlay matrix locked:** dev = 1 replica + emptyDir Postgres + no NetworkPolicy + plain Secret; staging = 2 replicas + PVC + NetworkPolicy enforced + plain Secret + AlertManager logs sink; prod = 3 replicas + HPA (lock-pool saturation + CPU fallback) + podAntiAffinity + topologySpreadConstraints + PDB minAvailable=2 + NetworkPolicy enforced + **SealedSecret REQUIRED** (plain Secret deleted via `$patch: delete` in `overlays/prod/patches/sealed-secret-required.yaml`). | Three rungs match the standard promotion ladder; each rung adds exactly one production property over the previous. prod refuses plain Secret at render time to prevent operator footgun. | (a) Two overlays (dev+prod): loses the staging integration point where AlertManager + NetworkPolicy can be exercised before prod; (b) Four overlays (add `canary`): premature — no signal of need yet; (c) Allow plain Secret in prod with warning: warnings get ignored; hard fail beats soft warn. |
| D-K8S-3 | 2026-05-18 | **Hardening parity with compose, mechanically translated.** Every Pod spec carries `runAsNonRoot: true`, non-root `runAsUser` (daemon=10001 matching Dockerfile, postgres=70, prometheus=65534, grafana=472, collector+jaeger=10001), `readOnlyRootFilesystem: true`, `allowPrivilegeEscalation: false`, `capabilities.drop: [ALL]`, `seccompProfile.type: RuntimeDefault`, `automountServiceAccountToken: false`, CPU + memory requests + limits. Postgres re-adds `CHOWN, DAC_OVERRIDE, FOWNER, SETGID, SETUID` to match alpine initdb requirements (parity with compose `cap_add` block). | Compose `cap_drop: [ALL] + read_only: true + no-new-privileges + ulimits.core: 0` is the established hardening baseline. The k8s translation must enforce equivalent invariants on every surface. Render test asserts these keys are present in every overlay. | (a) PSP (PodSecurityPolicy): deprecated in k8s 1.25+; (b) OPA Gatekeeper / Kyverno-only enforcement: external admission controller dependency for a reference template is excessive; (c) Trust the namespace PodSecurity-restricted label only: defense-in-depth — both labels + per-pod securityContext. |
| D-K8S-4 | 2026-05-18 | **NetworkPolicy enforces internal-only data flows.** Default-deny baseline per namespace. Explicit allows: daemon to postgres:5432, daemon to otel-collector:4317 (only when component applied), daemon to DNS, daemon to 0.0.0.0/0 :443 except RFC1918 (LLM provider egress); postgres ingress from daemon only on 5432; otel-collector ingress from daemon (4317) plus prometheus (8889). dev removes default-deny via `$patch: delete` for local debugging. | Closes lateral-movement vectors. Daemon can only reach postgres + collector + public LLM endpoints; no cluster-internal pivot. Postgres has no outbound dependency, so egress is DNS-only. | (a) Trust the CNI's per-namespace isolation: not all CNIs (Calico vs Cilium vs flannel) ship the same defaults; explicit NetworkPolicy is portable; (b) FQDN-based egress: requires DNS-aware policy engine; documented as operator-tightening option in README; (c) Allow daemon-egress wildcard: defeats the purpose. |
| D-K8S-5 | 2026-05-18 | **Secrets mounted as files, not env vars.** Daemon deployment env block uses `POSTGRES_PASSWORD_FILE` + `FERNET_KEY_FILE` pointing at `/var/run/secrets/durable/*`. Volume `defaultMode: 0400`, mount `readOnly: true`. dev = plain Secret; prod = SealedSecret via component. Daemon `*_FILE` shim documented in `base/secrets/README.md` (12-factor pattern). | Closes `/proc/self/environ` enumeration class. Container compromise that reads env vars no longer leaks secrets. Pattern matches compose `POSTGRES_PASSWORD_FILE: /run/secrets/postgres_password` from `examples/production/durable_postgres/docker-compose.yml`. | (a) `valueFrom: secretKeyRef`: env-var pattern; loses the file-mount win; (b) External Secrets Operator: extra dependency; documented in README as operator-tightening; (c) CSI Secret Store driver: cloud-specific; deferred to operator. |
| D-K8S-6 | 2026-05-18 | **HPA on lock-pool saturation (custom metric) with CPU fallback** in prod overlay. `metrics:` block declares Pods-metric `durable_lock_pool_saturation` target `700m` (70 percent) AND Resource-metric CPU target `70%`. minReplicas=3, maxReplicas=10. scaleDown stabilizationWindow=300s; scaleUp 100% plus 2 pods per 30s with `selectPolicy: Max`. | Lock-pool saturation is the workload-correct signal (per Tier 1.1 metrics surface). CPU fallback ensures HPA still functions when prometheus-adapter isn't installed. scaleDown stabilization prevents thrash on transient dips. | (a) CPU-only HPA: misses the lock-pool-bottleneck case where CPU is low but locks are saturated; (b) Custom-metric-only: HPA goes inert if prometheus-adapter missing; fallback restores graceful degradation; (c) KEDA: more powerful but extra controller dependency. |
| D-K8S-7 | 2026-05-18 | **PodDisruptionBudget protects paused-run holders.** PDB `minAvailable: replicas - 1` per overlay (dev=1, staging=1, prod=2). | Paused runs hold locks; rolling deploys + node drains must not evict all daemon replicas simultaneously. PDB is the k8s primitive for this. | (a) No PDB: a node drain could evict all replicas and the durable scheduler would leak locks until restart; (b) `minAvailable: 100%`: blocks all voluntary disruption; operator can never drain a node. |
| D-K8S-8 | 2026-05-18 | **Probe split: startup `/health`, readiness `/ready`, liveness `/live`** on daemon Deployment. startupProbe failureThreshold=30 times 5s (150s bootstrap budget); readinessProbe failureThreshold=3 times 5s; livenessProbe failureThreshold=3 times 10s. Daemon image must serve all three endpoints (sibling-level work; library unchanged). | Compose `/health` conflates the three; k8s probe semantics require separation or risks evicting busy-but-healthy daemons. Startup gives async pool plus scheduler time; readiness gates traffic; liveness restarts only on stuck event loop. | (a) Single `/health` for all three probes: existing-compose pattern; risks restart-on-bootstrap (slow asyncpg pool init) or traffic-during-bootstrap; (b) tcpSocket probes: too coarse; doesn't catch "tcp accepts but scheduler is dead" cases. |
| D-K8S-9 | 2026-05-18 | **OTel observability is a kustomize component**, not part of base. `components/otel/` carries collector plus jaeger plus prometheus plus grafana with ConfigMaps inlined (collector-config, prometheus.yml plus alerts.yml, grafana provisioning datasources plus dashboards). Operator opts in via `components: [../../components/otel]` in overlay kustomization. | Operators with cluster-wide observability stacks (Datadog, NewRelic, in-house Tempo plus Mimir plus Grafana) should not pay for duplicate components. Component pattern is the kustomize-native way to express opt-in. | (a) Make otel part of base: forces every operator to deploy 4 extra pods; (b) Separate top-level kustomization (`durable_postgres_k8s_otel/`): copy-paste duplication of base plus overlays; (c) Helm dependency chart: pulls in D-K8S-1 alternative we rejected. |
| D-HEALTH-1 | 2026-05-16 | **New domain: Healthcare Clinical Decision Support** under `src/adv_multi_agent/healthcare/`. 27-workflow catalog with 8 workflows in MVP: `DiagnosisCodeAuditWorkflow`, `DischargePlanningRiskWorkflow`, `PriorAuthorizationReviewWorkflow`, `ClaimsAppealReviewWorkflow`, `DrugInteractionFlaggingWorkflow` (veto), `AdverseEventTriageWorkflow` (veto), `TreatmentPlanReviewWorkflow` (veto), `ClinicalTrialEligibilityWorkflow` (veto + bias-gate). Phase-2 deferred (19 workflows). Same namespace + skill-prefix convention as retail/pc/industrial (`diagnosis_*`, `discharge_*`, `priorauth_*`, `claims_*`, `drug_*`, `adverseevent_*`, `treatment_*`, `trial_*`). Synthetic data only — PRODUCTION_GAPS docstring per workflow. No `HealthcareWorkflow` base class (D-RETAIL-7 reasoning applies). Design doc: [`docs/superpowers/specs/2026-05-16-healthcare-domain-design.md`](superpowers/specs/2026-05-16-healthcare-domain-design.md). | Healthcare concentrates the four ARIS properties: irreversibility (diagnosis codes → billing fraud liability; treatment plan rejects → patient harm; trial enrollment → informed-consent gate; adverse-event non-reporting → FDA 7/15-day expedited report + IRB audit), regulator audit (FDA 21 CFR 312 / ICH E2A serious ADR; IRB exclusion criteria; HIPAA privacy rule on PHI; state medical board; JAMA 2019 demographic-bias patterns in clinical triage), echo-chamber risk (precedent bias in drug interactions, diagnostic anchoring replicates in same-family LLM), asymmetric info (clinician expertise / patient comorbidities / site-specific protocols surface in different parts of the EMR). Veto count 4-of-8 (vs industrial's 2-of-8) reflects patient-safety + regulatory stakes (FDA 7/15-day, ICH E2A, IRB irreversibility). | (a) Build all 27 workflows: token-budget infeasible; design-locked Phase 2 allows deferral without cost; (b) Anchor on diagnostics instead of drug interaction: diagnostic coding is routine, less expressive of the adversarial-review value; (c) Combine drug-interaction + adverse-event into one workflow: distinct regulatory surfaces (FDA drug approval vs post-market pharmacovigilance) and gate shapes deserve separation. |
| D-HEALTH-2 | 2026-05-16 | Score threshold 8.0 for all veto-using healthcare workflows (DiagInteractionFlagging, AdverseEventTriage, TreatmentPlanReview, ClinicalTrialEligibility) vs 7.5 default for non-veto. Justification: patient-safety stakes + regulatory irreversibility (FDA 7/15-day serious-unexpected ADR report; ICH E2A dossier update; IRB audit + corrective action; state medical board investigation). A false-negative halt (approving a contraindicated drug, enrolling an ineligible trial subject) has documented harm (e.g. JAMA 2019 trial exclusion-criteria bias). The 0.5-point lift reflects the asymmetric cost of approver override when veto is triggered. | Threshold tuning is the precision lever when validator (reviewer) is operating at high cost per call. Healthcare veto-using workflows are the most sensitive to false-positive halts (clinician friction), so threshold elevation buys specificity. Tested empirically in parole (bias-gate threshold was set at 8.5) and validates against domain literature (JAMA 2019 equity-audit findings indicate bias detection FSP > 0.15 at generic thresholds). | (a) Single 7.5 threshold across all domains: loses the precision gain for patient-safety workflows; (b) Per-workflow threshold knob in the workflow class: adds config surface; D-HEALTH-2 codifies the principle so a new workflow inherits the pattern without a new config knob. |
| D-HEALTH-3 | 2026-05-16 | All patient-identifying fields (name, MRN, DOB, diagnosis, treatment detail, site, comorbidities) are caller-supplied free-text; PHI de-identification is caller's responsibility; every workflow docstring lists this as PRODUCTION_GAP #1 and names HIPAA Privacy Rule § 164.502(b) Safe Harbor de-identification standard. The workflow applies `sanitize_for_prompt` (shared helper) for prompt-injection defense, but cannot validate upstream de-identification. Workflows accept pre-de-identified clinical data only. | HIPAA Privacy Rule makes the covered entity (hospital, health plan, clinic) the de-identification accountable party, not the advice engine. A production deployment would require a HIPAA-certified de-identification appliance (e.g. Deidentification SDK, medical NLP de-ID tools) operating on input before the advisory loop receives it. Documenting the gap prevents a downstream consumer from mis-deploying the template without the de-ID layer. | (a) Attempt in-workflow re-identification checking: HIPAA Safe Harbor has 18 specific identifiers; detecting all 18 reliably in natural language is a solved problem only with medical NLP, not in a prompt template; (b) No mention of de-identification: risks a user deploying on live PHI without a HIPAA-certified de-ID appliance, creating audit/liability risk. |
| D-DURABLE-1 | 2026-05-16 | Schema-versioned checkpoints with strict-extra rejection | Pause/resume across days/weeks widens the surface for silent shape drift; explicit `schema_version` + unknown-key rejection makes drift fail loud. Related: D-HEALTH-3, M-PC-1. Files: `core/durable/checkpoint.py`, `core/durable/token.py`. | Silent schema migration (D-RETAIL-style `from_dict` filter): forward-compat across pauses is too long a horizon to silently absorb shape drift |
| D-DURABLE-2 | 2026-05-16 | Reconciliation hook is a caller-trusted Protocol; resume re-sanitization is caller's responsibility | Library cannot know whether the world has moved during the pause; hook is the seam where caller-owned freshness logic plugs in. The hook executes inside the library's trust boundary, so its output is treated as caller input — same trust as the original `Request` construction. Related: D-HEALTH-3 (PHI caller responsibility extends across time). Files: `core/durable/hooks.py`, `core/durable/workflow.py` resume(). | Library-side sanitization of hook return value: would force a per-Request-type sanitizer registry; misaligned with composition design |
| D-DURABLE-3 | 2026-05-16 | Pluggable storage/lock/scheduler Protocols; POC ships file + in-memory; production swaps Postgres/Redis | Forces the abstraction to hold from day one (Memory + File parametrized contract tests catch leakage). Avoids the rewrite-at-scale failure mode where the POC encodes file semantics into the public API. Related: D-RETAIL-7 (no domain base class — pattern reuse). Files: `core/durable/protocols.py`, `tests/unit/durable/test_checkpoint.py`. | POC-only file impl with refactor-later: every prior "we'll refactor later" decision became "we ship with the wrong abstraction" |
| D-HEALTH-4 | 2026-05-16 | Veto trigger language in healthcare reviewer criteria templates references specific regulatory citations, not generic "safety concern" phrasing. Examples: `FDA 21 CFR 312.21 7/15-day expedited report [serious unexpected ADR]`, `ICH E2A life-threatening / permanently disabling`, `IRB exclusion criteria per local protocol`, `JAMA 2019 demographic bias patterns [protected class proxy]`. Same pattern as industrial (D-IND-1 names CPSC § 15(b), OSHA 1904). The specific citations create audit-trail verifiability: a regulator reviewing the veto log can cross-reference the stated criterion to a published standard, not reverse-engineer intent from generic text. | Regulatory audit in healthcare is higher-stakes than retail/industrial (FDA Office of Scientific Integrity has enforcement authority; state medical boards have license-revocation authority). Naming the specific standard (rather than "potential bias" or "safety concern") makes the decision defensible. The templates are discoverable (in the skill markdown files) and auditable by domain experts. | (a) Generic "safety concern" language: makes veto decisions look ad-hoc and opinion-based rather than evidence-anchored; (b) No citation in veto criteria, names added only in operator runbooks: audit trail is incomplete — the runner of the workflow sees the veto but has to consult a separate runbook to understand the regulatory basis. |
| D-DURABLE-4 | 2026-05-16 | At-rest encryption is opt-in via Cipher Protocol decorator; library ships no built-in cipher | Bundling a cipher creates a "ship the example key" footgun AND a hard dependency on cryptography/Fernet. Caller composes: `EncryptedCheckpointStore(inner=BaseStore(...), cipher=...)`. Production callers wrap Fernet / AWS KMS / GCP KMS / Vault transit. Healthcare deploys MUST wrap. Related: D-HEALTH-3, H-DUR-4. Files: `core/durable/encryption.py`, `core/durable/protocols.py`. | Library-bundled Fernet default: ships an example key that callers copy into production; forces a hard dependency on cryptography. Mandatory encryption: prevents POC usage without a cipher; breaks the "library ships zero cipher" composability promise. |
| D-DURABLE-5 | 2026-05-17 | Workflow-version pinning policy. Hash = sha256(module + qualname + sorted(workflow_version_inputs()))[:16]. Soft-add field (no schema_version bump); back-fill on first resume with UserWarning. Drift defaults to pause+WORKFLOW_VERSION_DRIFT; force-accept via `force_workflow_upgrade=True` kwarg on resume() logs to rounds_history. `DURABLE_REFUSE_UNVERSIONED=1` env var hardens post-migration. | 21 CFR Part 11 attestation requires the same prompt produce all rounds in a run; a code deploy mid-flight silently changes recommendations without a hash guard. Soft-add + back-fill avoids a schema_version bump for existing checkpoints (D-DURABLE-1). Force-accept with audit-log entry gives operators an escape hatch without silently breaking the chain. | Hard-reject unversioned checkpoints immediately: breaks existing deployed runs before operators can migrate; too abrupt for a soft-launch feature. Hash over all library code: too broad — version drift on an unrelated module would spuriously block resume. |
| D-PROD-1 | 2026-05-16 | Reference deployment lives in `examples/production/`, not in the library | Preserves zero-infra-dependency stance for the library. Mirrors `examples/healthcare/` etc. patterns. Library ships zero asyncpg / cryptography / docker dependency. Production callers clone the example and adapt. Files: `examples/production/durable_postgres/*`. | Bundle Postgres impls into `adv_multi_agent.durable.postgres`: forces optional install extra, couples library to asyncpg, becomes the "ship the example key" footgun D-DURABLE-4 explicitly avoided. |
| D-PROD-2 | 2026-05-16 | `examples/production/` enforces asyncpg parameterized queries only; f-string SQL is grep-gated; defense-in-depth via DB CHECK constraint mirroring app-layer regex | SQL injection is the #1 prod-deploy risk. Stacking app-layer regex (library `_RUN_ID_RE`) + DB-layer CHECK constraint + grep gate (`scripts/check_no_fstring_sql.sh`) + bandit B608 (`scripts/audit_deps.sh`) gives four independent layers. Defense in depth catches future regressions. Files: `examples/production/durable_postgres/store.py`, `lock.py`, `schema.sql`, `scripts/check_no_fstring_sql.sh`, `scripts/audit_deps.sh`. | Single-layer (regex only OR check constraint only): one bug in any layer ships a SQL injection to prod. |
| D-PROD-3 | 2026-05-16 | `examples/production/` ships with: SQL-injection grep gate · non-root + read-only-rootfs + no-cap container · hashed dep lockfile · SBOM + audit-gate · key-material redaction across repr / logs / healthcheck | Reference deployment models production posture concretely. Each control maps to a smoke-test assertion (spec §10.1). Reader copies the pattern to their own deploy. Files: `Dockerfile`, `docker-compose.yml`, `requirements.txt`, `cipher.py` (repr redaction), `daemon.py` (log allowlist + healthcheck key lock). | Ship the reference deployment without hardening: reader copies a fundamentally-insecure baseline into prod. |
| D-CIPHER-GCP-1 | 2026-05-17 | Envelope encryption pattern: per-run DEK generated by KMS `GenerateDataKey`, encrypted locally with AES-256-GCM, wrapped DEK stored alongside ciphertext in `GKMSv1:<wrapped_dek>:<nonce>:<ct>` format. KMS is used only to wrap/unwrap DEK, not to encrypt payloads directly. | KMS-direct encryption has a 64 KiB per-call limit — checkpoint payloads can exceed this for long multi-round runs. Envelope pattern removes the size constraint; DEK rotation is implicit (each new run gets a new DEK); per-op cost is dominated by the single KMS wrap/unwrap call, not per-byte pricing. | KMS-direct payload encryption: 64 KiB limit is a hard wall for large checkpoints; per-byte billing at KMS rates would be expensive for multi-round runs with large prompt histories. |
| D-CIPHER-GCP-2 | 2026-05-17 | ADC (Application Default Credentials) for GCP authentication in `GcpKmsCipher`. No JSON key files in repo, compose, or process environment. Local dev uses `gcloud auth application-default login`; GKE / Cloud Run use Workload Identity Federation. | ADC is the GCP-recommended credential pattern. JSON key files are a persistent credential that outlives any single session, require manual rotation, and are frequently leaked via compose mounts or docker build context. Workload Identity removes long-lived credentials from the picture entirely for cloud deployments. | JSON key file in compose `volumes`: simple to set up, but creates a long-lived credential at rest on the host; `--no-verify` during docker build can accidentally include it in the image layer. Service Account impersonation without ADC: works but requires more setup with no security benefit for the target use case. |
| D-CIPHER-GCP-3 | 2026-05-17 | Synchronous KMS path for v1; `GcpKmsCipher.encrypt` and `.decrypt` are sync methods. The library's `EncryptedCheckpointStore.write/read` bridge to async via `asyncio.to_thread` (Task 0 fix). Async `Cipher` Protocol deferred until real workload contention surfaces. | KMS Decrypt p99 latency is ~30 ms — tolerable for the durable checkpoint use case where calls are infrequent (one decrypt per run resume, not per round). `asyncio.to_thread` correctly prevents event-loop blocking without requiring a new async Protocol variant in the library. Adding an `AsyncCipher` Protocol now would add a two-Protocol surface that every caller must navigate before any evidence of real contention. | AsyncCipher Protocol in library now: no real workload showing contention; premature abstraction (CLAUDE.md anti-pattern). Blocking sync KMS calls in event loop without `asyncio.to_thread`: blocks event loop on network I/O during checkpoint write/read — measurably wrong (task 0 test validates). |
| D-CIPHER-GCP-4 | 2026-05-17 | `CIPHER_BACKEND=fernet\|gcp_kms` runtime env var selects cipher at daemon startup. Single Docker image ships both ciphers; operator toggles via `.env`. Neither backend is the default — omitting the var raises `ValueError` at startup (fail-loud). | Single-image multi-backend enables future migration path: read existing Fernet-encrypted rows (`CIPHER_BACKEND=fernet`), write new rows with GcpKms (`CIPHER_BACKEND=gcp_kms`) — no image rebuild needed to straddle the migration window. Fail-loud on missing var prevents silent plaintext fallback. | Separate Docker images per backend: doubles CI matrix, adds image management complexity with no security benefit. Defaulting to one backend: risks silent wrong-cipher selection when operator forgets to set the var; plaintext-fallback footgun. |
| D-OTEL-1 | 2026-05-18 | `MetricsBackend.span(name, tags)` as async context manager; minimal `Span` Protocol with `set_attribute` + `record_exception` only. Library default `_NoopSpan` (zero overhead). Wired around per-round loop in `DurableWorkflow.start()` + `resume()`. Spec: `docs/superpowers/specs/2026-05-18-otel-deployment-design.md` section 3. Files: `src/adv_multi_agent/core/durable/metrics.py`, `src/adv_multi_agent/core/durable/durable.py`. | Async ctx mgr is natural fit for async workflow code; decorator-based span would need a method seam the library does not otherwise need. Minimal Protocol surface keeps adapters trivial (sibling `_OtelSpan` is ~30 lines). | Pass-through tracer object: leaks vendor types across library boundary; library no longer OTel-dep-free. Sync span: forces callers to thread-pool or rewrite for sync — breaks async-throughout convention. |
| D-OTEL-2 | 2026-05-18 | PII redaction lives in sibling `SpanProcessor` with explicit `_ALLOWED_ATTRS` frozenset; non-allowlisted span attrs stripped before export; exception events keep `exception.type`, drop message + stacktrace. Library forbids per-request PII in metric tags via fixture test (D-OTEL-4); no runtime metric-tag redaction (tags hit Prometheus before any processor can intervene). Files: `examples/production/durable_postgres_otel/pii_redaction_span_processor.py`. | Defense in depth for caller bugs + accidental exception attribute leakage. Library cannot know caller PHI shape; allowlist is the safest default. Metric-tag discipline enforced at test time because Prometheus has no equivalent processor seam — wrong tag value means unrecoverable cardinality blowup plus PHI bleed. | Regex-based PII redaction: same shape that broke twice (M-PC-1, H-IND-1). Library-side redaction: forces library to know OTel API + ship cryptography dep; violates D-PROD-1. |
| D-OTEL-3 | 2026-05-18 | OTel Collector runs as sibling container in `docker-compose.yml`. Daemon exports via OTLP gRPC to `otel-collector:4317`. Collector handles batching, retry, backend fan-out (Jaeger traces, Prometheus metrics). Operator chooses production backend by editing `collector-config.yml` exporters — no daemon redeploy needed. Files: `examples/production/durable_postgres_otel/docker-compose.yml`, `collector-config.yml`. | Daemon-embedded exporters couple daemon to every backend the operator might choose. Sidecar collector is OTel-canonical pattern; vendor swaps (Honeycomb / Datadog / Tempo) are config-only. Batching + retry move out of daemon hot path. | Daemon-embedded OTLP exporter pointing directly at vendor: per-vendor daemon image variants; rebuild on backend swap. Push to library: library ships OTel client; breaks D-PROD-1 separation. |
| D-OTEL-4 | 2026-05-18 | Tag-value cardinality budget enforced by runtime fixture test (`RecordingMetricsBackend` + `tests/unit/durable/test_metrics_cardinality.py`), not grep-based static gate. Test exercises every wire point and asserts emitted tag keys are spec-allowlisted + tag values are within bounded-cardinality sets. Files: `src/adv_multi_agent/core/durable/metrics.py`, `tests/unit/durable/test_metrics_cardinality.py`. | Grep gate has the same failure shape as M-PC-1 + H-IND-1: regex matches the literal `tags={...}` syntax but misses tag values computed at runtime from per-request variables. Fixture test instantiates the workflow and inspects emitted tags — catches the runtime form. Per CLAUDE.md convention-level-error-compounding rule. | Static regex grep gate: same shape that already broke twice. Runtime sampling in production: too late; first PHI value already in Prometheus retention. |
| D-OTEL-5 | 2026-05-18 | OTel SDK + collector + Jaeger + Prometheus + Grafana deps confined to `examples/production/durable_postgres_otel/requirements.in` + sibling `pyproject.toml`. Library `pyproject.toml` ships zero OTel dependency. Callers opt in by deploying the sibling stack OR writing their own `MetricsBackend` adapter against any vendor SDK. Files: `examples/production/durable_postgres_otel/pyproject.toml`, `requirements.in`. | Keeps library install footprint small (current direct deps: anthropic, openai, pydantic, python-dotenv). OTel SDK pulls grpcio + protobuf + ~10 transitive deps; bundling doubles install size for callers using logs-only observability. Sibling pattern mirrors D-PROD-1 (production reference is opt-in clone-and-adapt, not import). | Library-bundled OTel exporter: doubles install footprint; couples library release cadence to OTel SDK release cadence. Optional extras `pip install adv-multi-agent-products[otel]`: possible but obscures dependency surface. |
| D-AEAD-1 | 2026-05-18 | Full-Checkpoint integrity tag covers every field except `integrity_tag` itself. Computed as `SEAL:v1:<run_id>:<schema_version>:<sha256_of_canonical_json>` then encrypted via caller-supplied Cipher. Tag stored on `Checkpoint.integrity_tag: str | None`. Closes A10-H2. Files: `core/durable/encryption.py`, `core/durable/checkpoint.py`. | Single tag binding to the post-encryption form gives one verify-step instead of per-field MACs; binding to all fields prevents the cycle-10 insider-tamper class on workflow_version_hash / rounds_history / status. Spec: `docs/superpowers/specs/2026-05-18-full-checkpoint-aead-design.md`. | Per-field MAC: O(N) verify cost + N tag-storage columns; same security with worse ergonomics. Hash-only (no Cipher): fails to authenticate (attacker recomputes hash); requires HMAC key separate from encryption key. |
| D-AEAD-2 | 2026-05-18 | Canonical JSON via `json.dumps(asdict(cp), sort_keys=True, separators=(",", ":"), ensure_ascii=False)` with `integrity_tag` popped. Deterministic across Python versions. Files: `core/durable/encryption.py` `_canonical_checkpoint_bytes`. | `sort_keys=True` removes dict-insertion-order dependence; compact separators remove whitespace ambiguity; `ensure_ascii=False` keeps UTF-8 stable. Excluding `integrity_tag` from canonical bytes prevents the trivially-fixed-point problem (tag would include itself). | Pickle: non-deterministic across Python versions; security risk on read. MessagePack: extra dep + same canonicality concern unless `use_single_float=True`. CBOR-canonical: better but adds dep without payoff at current shape. |
| D-AEAD-3 | 2026-05-18 | `IntegrityViolation` raises with `expected_hash` + `observed_hash` truncated to 32 chars; NO field values, NO PHI, NO key material. Files: `core/durable/protocols.py`. | Operator needs enough signal to investigate (which run, which hash mismatched); leaking field values would expose the very PHI the encryption is designed to protect. 32-char hash prefix is enough for cross-referencing audit-log entries without enabling a side-channel. | Full hash in exception: exposes pre-image attack surface to log scrapers. Field-value dump in exception: re-leaks PHI through error-handling paths. |
| D-AEAD-4 | 2026-05-18 | Pre-1.9 rows (integrity_tag=None) accepted on read with `LegacyPartialAEADWarning`; auto-reseal on next library write. Bulk migration via `reseal_all_checkpoints.py`. Files: `core/durable/encryption.py`, `examples/production/durable_postgres/scripts/reseal_all_checkpoints.py`. | Forces no flag-day migration; long-paused runs (days-to-weeks horizon per CLAUDE.md durable scope) continue resuming without operator intervention. UserWarning subclass surfaces the gap in operator logs. | Reject pre-1.9 rows on read: breaks every paused run at deploy time. Silent accept without warning: operator never learns the migration is pending. |
| D-AEAD-5 | 2026-05-18 | Hash-round-trip invariant: reseal MUST preserve `workflow_version_hash` byte-for-byte. Verified by `reseal_one` outcome `workflow_version_hash_preserved` field; smoke test asserts; CLI exits non-zero if any row breaks. Files: `examples/production/durable_postgres/scripts/_reseal_helpers.py`, `test_reseal_smoke.py`. | If reseal mutated workflow_version_hash, every paused run would pause with WORKFLOW_VERSION_DRIFT at next resume — operator-visible regression at scale. Reseal is signing, not re-versioning. | Skip the invariant check: regression escapes to production. Bake into library write(): library should not know about reseal-vs-normal-write distinction. |
| D-AEAD-6 | 2026-05-18 | `reseal_all_checkpoints.py --dry-run` is DEFAULT; `--apply` is explicit opt-in. Uses `PostgresCheckpointStore.write_if_unchanged` (CAS on `updated_at`) for optimistic concurrency. Files: `examples/production/durable_postgres/scripts/reseal_all_checkpoints.py`. | Destructive default would let an operator clobber concurrent daemon writes on the first careless invocation; CAS prevents the silent overwrite when a daemon write lands between the script read and write. Same pattern as the existing `reencrypt_all.py`. | `--apply` default: aligns with `rm -rf` family of footguns. Blind UPSERT without CAS: races the live daemon during a long sweep; data loss. |
| D-SCHEMA-1 | 2026-05-18 | Schema-migration registry lives in library at `core/durable/schema_migrations.py` and ships EMPTY at v1. `REGISTRY: dict[int, Callable[[dict], dict]]` keyed by SOURCE version; `chain_migrations(row, target_version)` is the primitive the offline tool calls. Established convention (Tier 1.6 + Tier 1.9) is **additive-only** (nullable field + deserializer exemption + no version bump); registry exists for the cases that convention cannot cover (rename, drop, non-coercible type change, semantic shift, rounds_history reshape, nullable-to-required). Files: `src/adv_multi_agent/core/durable/schema_migrations.py`, `tests/unit/durable/test_schema_migrations.py`. | Library-side registry avoids forcing every operator to copy-paste chain-migrations into their deployment script. Empty at v1 is honest: no real v2 schema exists yet, and inventing a synthetic migration would create a maintenance burden without exercising a real change. Mechanism is tested via 5 monkeypatched-fixture tests. Tier 1.9's seam reasoning (Cipher byte primitive vs AEAD-of-row) does NOT apply — `chain_migrations` IS the right primitive. | Per-domain registries: migrations are shape-level not domain-level. Ship with a synthetic v1→v2: invents maintenance burden without exercising real change; advisor flagged as premature. Class-based `CheckpointMigrator` from runbook sketch: heavier than needed; module-level dict + function is exactly the surface required. |
| D-SCHEMA-2 | 2026-05-18 | Migration script lives in `examples/production/durable_postgres/scripts/migrate_schema.py` and mirrors the `reseal_all_checkpoints.py` shape: `--dry-run` default, `--apply` explicit, optimistic-CAS via `PostgresCheckpointStore.write_if_unchanged`, exits 0 clean / 1 CAS conflicts / 2 abort. Pure helper extracted to `_migrate_helpers.py` for unit testability. Files: `examples/production/durable_postgres/scripts/migrate_schema.py`, `_migrate_helpers.py`, `test_migrate_schema_smoke.py`. | Same shape as the reseal + reencrypt scripts so operators learn one pattern. Pure-helper extraction lets the smoke test run without Postgres (4 tests, no DB). Keeps Postgres-specific code in the deployment sibling and the migration mechanism in the library — same separation of concerns as the cipher / store / scheduler protocols. | Library-side CLI: leaks deployment-specific shape (`asyncpg`, DSN) into the library. Inline helper in the script: loses unit-test coverage. |
| D-SCHEMA-3 | 2026-05-18 | Synthetic v1→v2 migration registered IN THE TEST via `monkeypatch.setitem(REGISTRY, 1, _fake_v1_to_v2)`; the library REGISTRY stays empty at runtime. Mechanism tests cover: empty-registry no-op, single-step migration, missing-migration error, broken-migration (wrong return version) error, multi-step v1→v3 ordering. Files: `tests/unit/durable/test_schema_migrations.py` (5 tests). | Monkeypatch isolation lets the mechanism be exercised without inventing a v2 schema the library would then have to maintain forever. Each test asserts a single failure mode; the multi-step test proves migration ORDER (not just that both run). When real v2 lands, the real migration test sits alongside; synthetic stays as mechanism coverage. | Real synthetic v2 baked into the library: every additive change after that would have to also touch the migration, creating phantom maintenance. Only the no-op case: would not catch missing-migration / broken-migration / ordering bugs (the three most likely authoring mistakes). |
| D-SCHEMA-4 | 2026-05-18 | `migrate_schema.py --dry-run` is DEFAULT; `--apply` is explicit opt-in. Matches `reseal_all_checkpoints.py` + `reencrypt_all.py` precedent. Exit codes: 0 clean, 1 CAS-conflicts (some rows skipped, re-run safe), 2 abort (future-version row, missing migration, broken migration — stop the sweep). Files: `examples/production/durable_postgres/scripts/migrate_schema.py`. | Consistency with existing destructive-tool defaults so operators have one mental model. Exit-code split lets shell-level orchestration distinguish "retry-safe" (1) from "human-intervention-required" (2). | `--apply` default: every other destructive tool defaults to dry-run; deviating here would be a footgun. Single non-zero exit: loses operator signal about whether the partial migration is safe to retry. |
| D-SCHEMA-5 | 2026-05-18 | Forward-only: tool aborts the sweep when ANY row has `schema_version > target_version` (downgrade scenario after a library rollback). Aborts BEFORE mutating any row — partial-mutated DB is the worst failure mode. `_main` also rejects `--target-version > CURRENT_SCHEMA_VERSION` since the library cannot synthesize migrations to versions it does not know. Files: `examples/production/durable_postgres/scripts/_migrate_helpers.py` (`FutureVersionError`), `migrate_schema.py` (early sweep abort). | A future-version row means the operator rolled back the library after a newer version wrote rows. The right response is "stop and ask a human" — silent downgrade attempt would either lose fields the older library doesn't know about, or corrupt the row by writing back fields with mismatched semantics. CLAUDE.md autonomy hard-stops include schema migrations with data backfill on live DB; this is the in-tool guard for that policy. | Best-effort downgrade: irreversibly loses data added by the newer version. Continue past the future-version row: leaves a mixed-version DB after the sweep, which the post-sweep verify step would miss because the row "is already at a version >= target". |
| D-BACKUP-1 | 2026-05-18 | **Client-side `age` encryption BEFORE upload** (defense in depth over bucket SSE). Backup pipeline: `pg_dump` → `age --recipients-file recipients.txt` → cloud upload. Recipients file holds PUBLIC age keys only (sanity check refuses any `AGE-SECRET-KEY-` content). Same recipients cover base backups + WAL archive segments — bucket leak yields ciphertext only. Files: `examples/production/durable_postgres/scripts/backup.sh`, `recipients.txt`, `postgresql.conf.snippet`. | Object-store SSE is the operator's first line; client-side encryption is the second. `age` chosen over `gpg` for simpler recipient-key custody (no web-of-trust), single binary, public-key pattern matches "private key lives offline" model. | SSE-only: a single misconfigured bucket policy (public read) exposes plaintext. `gpg`: heavier key-handling surface; web-of-trust irrelevant to our model. Server-side KMS-only encryption: ties recovery to cloud KMS availability + IAM scope intact. |
| D-BACKUP-2 | 2026-05-18 | **`STORAGE_BACKEND` env switch** (`s3` / `gcs` / `azure-blob` / `file`) routes upload + download. No `--provider` flag. Cloud creds read by the respective CLI (`aws` / `gsutil` / `az`) from ambient session / instance role — script NEVER reads or prints them. **Default is `file`** to force operator awareness before going to prod. Files: `backup.sh`, `restore.sh`. | 12-factor pattern: env drives infra selection; same script binary works across clouds. Default `file` prevents the common "ran the script with defaults and silently wrote to /tmp" footgun for a production-bound tool. | Per-cloud script variants: triples maintenance + drift surface. `--provider` flag default to `s3`: hard-codes AWS preference; surprise for GCP / Azure operators. Default to `s3` regardless: an operator who forgets to set the bucket will exit with cryptic error or worse silently fail. |
| D-BACKUP-3 | 2026-05-18 | **Continuous WAL archiving via Postgres `archive_command`** is the PITR backbone. `postgresql.conf.snippet` sets `wal_level=replica`, `archive_mode=on`, `archive_timeout=60`, and pipes each segment through `age` (same recipients as base backups) into the same bucket. `scripts/setup_wal_archiving.sh` is PRINT-ONLY (operator owns `postgresql.conf` custody). Worst-case RPO = 1 WAL segment (~16 MB, ~minutes); base-backup-only RPO = ≤ 24 h. Files: `postgresql.conf.snippet`, `scripts/setup_wal_archiving.sh`. | Daily `pg_dump` alone pins RPO at ≤ 24 h, which exceeds the "single failure invalidates value prop" close-out target. WAL archiving is Postgres-native, doesn't require streaming-replica infrastructure, and inherits the same encryption recipients as base backups. | Streaming replica only: requires a second always-on Postgres host + replication slot management; more infra than a teaching reference can prescribe. Daily-only: leaves a 24h RPO window. Logical replication: rejected as out of scope (physical pg_dump + WAL is the reference path). |
| D-BACKUP-4 | 2026-05-18 | **Integrity verification on restore** is mandatory, not optional. `restore.sh` runs three checks post-`pg_restore`: (a) Postgres responds to `SELECT 1`, (b) `SELECT COUNT(*) FROM checkpoints` matches `manifest.checkpoint_count`, (c) `integrity_tag` round-trips on 10 random checkpoints via `verify_integrity_sample.py` (imports `adv_multi_agent.core.durable.encryption.EncryptedCheckpointStore`). Any failure → non-zero exit; restore considered failed; operator pulls older blob. Files: `restore.sh`, `verify_integrity_sample.py`. | Backup integrity is meaningless if tampered backups would be accepted silently — Tier 1.9 follow-through. The integrity sample exercises the FULL chain (read row → decrypt tag with caller's Cipher → recompute canonical hash → verify) so a forged blob fails closed instead of getting promoted. 10 samples is the lever: each sample is one Cipher decrypt + SHA-256; 10 keeps verification under ~1s for KMS-backed ciphers. | Count-only verification: doesn't catch tampered payloads, only truncation. Full-table integrity sweep: O(N) cipher decrypts; for ≥10k rows + KMS-backed cipher this exceeds the 2h RTO budget. Skip integrity verification: fails the close-out target. |
| D-BACKUP-5 | 2026-05-18 | **Monthly restore-drill cadence**, operator-owned, log lives in `docs/runbooks/restore-drill-log.md` (created by first drill — not in repo until then). Runbook ships a 7-step checklist for each drill (pick yesterday's blob, isolated staging Postgres, dry-run, confirmed restore, verify 3 steps, manually inspect 1 checkpoint, append log row). RPO targets: ≤ 1 WAL segment under PITR; ≤ 24 h under base-only. RTO target: ≤ 2 h. Files: `docs/runbooks/durable-backup-restore.md` §1, §6. | Untested backups are not backups. Monthly cadence is the standard from SRE Workbook ch. 9; tighter (weekly) overinvests human time without proportional confidence gain at our scale; looser (quarterly) lets the integrity sample silently regress between drills. Log file as operator-owned (vs in-repo) avoids "we ran the drill but didn't commit" noise. | Quarterly drills: too slack — bug introduced in month-2 sits live until month-3. Continuous drill via CI: would require a sacrificial Postgres + key custody in CI, which is more than the teaching template should prescribe. No documented cadence: same outcome as no drills. |
| D-BACKUP-6 | 2026-05-18 | **Sibling `manifest.json` per backup** captures `backup_id`, `timestamp`, `schema_version`, `checkpoint_count` (read from live DB via `SELECT COUNT(*) FROM checkpoints`), `wal_segment_at_backup` (`pg_walfile_name(pg_current_wal_lsn())`), `age_recipients` list, `tool_version`. Restore reads manifest FIRST and uses `checkpoint_count` for verification step (D-BACKUP-4 item b). Manifest uploaded alongside the encrypted dump under the same backup_id prefix. Files: `backup.sh`, `restore.sh`. | Verification requires a pre-restore expected value; embedding it in the dump means it can't be read before pg_restore. A sibling manifest is the smallest-surface answer: plaintext JSON (no sensitive content — just counts + WAL segment + public-key list) the restore script can `cat` before touching the target DB. WAL-segment field gives the operator the exact starting LSN for PITR replay. | Embed manifest in dump: chicken-and-egg with the verification it gates. Operator-supplied expected count via `--expected-count` flag: shifts a verification-critical value out of band into operator memory; failure mode is "operator typo'd, restore silently passed." Manifest in a separate bucket: doubles permission surface. |
| D-API-1 | 2026-05-18 | **Tier 2.2 — `EncryptedCheckpointStore.seal(cp)` is the public primitive; no parallel `rotate(new_cipher)` method.** Both reach-throughs from operator scripts (reencrypt + reseal) needed the same transform: "produce post-encrypt + post-integrity-tag form without writing." `seal()` is that pure transform; CAS composition stays in the deployment script where `inner.write_if_unchanged` lives. Files: `core/durable/encryption.py`. | One primitive, two callers — avoids the library knowing about deployment-specific `write_if_unchanged` semantics. Gaps doc §2.2 wording mentioned `rotate(new_cipher)` but that would force CAS coupling into the library; rejected per advisor review. | Public `rotate()`: forces library to know about inner.write_if_unchanged (impl detail of Postgres sibling, not Protocol). Shipping both: parallel public methods is API bloat. |
| D-API-2 | 2026-05-18 | **All module-level private reach-throughs closed via `seal()`.** Operator scripts previously imported `_compute_integrity_payload` and `_replace_integrity_tag` directly from `encryption.py`; `seal()` absorbs both. Verified zero `from adv_multi_agent.core.durable.encryption import _` anywhere outside the library. Files: `examples/production/durable_postgres/scripts/reseal_all_checkpoints.py`, `reencrypt_all.py`. | The seal/unseal primitives are useless if scripts still reach through to integrity helpers — the same forward-compat hazard would persist. Audit + close in the same lane. | Leave the helper imports as is: same A8-H-06 reach-through bug class, deferred remediation. Mark them as semi-public: dilutes the public/private split this lane exists to draw. |
| D-API-3 | 2026-05-18 | **Miserly export list — 14 net additions to `__all__`** (Checkpoint, CheckpointStore, IntegrityViolation, LegacyPartialAEADWarning, RunNotFound, SchemaVersionMismatch, RunLock, LockHandle, RunLocked, MemoryRunLock, FileRunLock, SchedulerBackend, HasWorkflowVersionInputs, chain_migrations + MissingMigrationError + BrokenMigrationError, FileCheckpointStore, MemoryCheckpointStore). Deferred: BudgetTracker family + CheckpointCorrupt (no current operator reach). Files: `core/durable/__init__.py`. | Each symbol added is a forward-compat obligation through the next major bump. Add only what operator code already imports or plausibly needs. Be willing to expand later (additive = minor bump). | Export everything for completeness: locks future refactor freedom for symbols nobody depends on. Export only the originals (10): forces operators to keep reaching into submodules for Checkpoint, IntegrityViolation, etc. — defeats the lane's purpose. |
| D-API-4 | 2026-05-18 | **Signature pin via `inspect.signature(...)`, not just name pin**, in `tests/unit/durable/test_public_api_stability.py`. Snapshots: (1) exact `__all__` set; (2) load-bearing operator-facing signatures (`EncryptedCheckpointStore.__init__`, `.seal`, `.unseal`, `chain_migrations`); (3) `Checkpoint` + `ResumeToken` dataclass field tuples. Adding optional kwargs with defaults is allowed (backward-compatible); test asserts EXACT current signature. Files: `tests/unit/durable/test_public_api_stability.py`. | Pinning `__all__` membership alone lets a silent kwarg rename slip through — exactly the bug class A8-H-06 surfaced. Signature pin catches it at PR time + forces conscious semver classification in the commit body. | Doctest-based pin: dies if module is renamed; harder to diff. Snapshot via pickled API: opaque, can't be code-reviewed. Field-by-field unit tests: high maintenance, same coverage in fewer LOC via inspect.signature. |
| D-API-5 | 2026-05-18 | **Semver policy doc at `docs/semver-policy.md`** — patch (bug fixes only), minor (additive: new symbols, new optional kwargs, new dataclass fields with defaults), major (removals, renames, required-arg adds). Cross-referenced from README "Stability" + SECURITY_MODEL §4. The pin test from D-API-4 enforces the contract at PR time. Files: `docs/semver-policy.md`. | Without a written contract, the pin test is just a checksum. Operators need to know what `0.x → 0.x+1` is allowed to do so they can pin `>=0.x,<0.x+1` safely. | Inline in CLAUDE.md: harder for operators to discover; CLAUDE.md is Claude-facing, not operator-facing. Implicit via release notes: works once releases exist, doesn't bootstrap; this lane ships pre-1.0. |
| D-BUDGET-1 | 2026-05-18 | **Tier 2.3 — `DurableWorkflow.acknowledge_budget_exceeded(token)` is the recovery primitive for `status="budget_exceeded"` rows.** Library performs the atomic flip (status budget_exceeded->paused) plus reseal via `store.write()` so the integrity_tag is recomputed against new canonical bytes. Operator must construct a new DurableWorkflow with a higher-cap BudgetTracker BEFORE calling. Audit row appended to rounds_history with budget snapshot at acknowledge time. Files: `core/durable/workflow.py`. | A regression probe confirmed raw operator status edits leave the integrity_tag stale, so the next EncryptedCheckpointStore.read raises IntegrityViolation. Therefore the recovery path MUST go through the library. Narrow method (one parameter, single transaction) keeps the API surface minimal. | Runbook-only fix (option 1 per advisor): impossible post-Tier-1.9 because raw status edit breaks integrity_tag. Full `resume_after_budget_exceeded(new_max_usd=...)` (option 3): couples library API to BudgetTracker constructor kwargs and creates a code path an insider could use to bypass budget enforcement invisibly. |
| D-BUDGET-2 | 2026-05-18 | **Reject `resume_after_budget_exceeded(token, new_max_usd=...)` convenience method.** The cap-raise stays in deployment code where it's reviewable; the library method only unblocks the status gate. | Coupling library API to BudgetTracker kwargs (max_tokens_in, max_tokens_out, max_usd, future per-tenant caps) would force every BudgetTracker change to ripple through DurableWorkflow's signature. Insider abuse: a single-call resume-with-new-cap hides the cap raise inside a library invocation; constructing the new BudgetTracker explicitly in deployment code is auditable in code review. | Single convenience method: smaller user-facing API but worse audit trail and tighter coupling. |
| D-BUDGET-3 | 2026-05-18 | **Audit row `{event: "budget_cap_acknowledged", at: <ts>, budget_used_at_ack: {...}}` is appended unconditionally to rounds_history on every successful acknowledge.** Carries the budget snapshot at acknowledge time. Compliance reviewers can reconstruct cap-raise decisions by joining this trail with deployment logs. | 21 CFR Part 11 / SOC 2 audits require provenance for any cap-modification event. The snapshot lets reviewers verify whether the next round's spend was tight against the new cap or another runaway. | Optional flag to skip the audit row: defeats the compliance purpose. Separate audit log table: introduces a second source of truth. |
| D-BUDGET-4 | 2026-05-18 | **Per-tenant budget enforcement deferred until Tier 2.1 (multi-tenant isolation) ships.** Today there is no tenant_id on the checkpoint or run, so per-tenant caps have nothing to key on. Gaps doc §2.3 explicitly defers ("Per-tenant budget after 2.1 lands"). | Premature abstraction: a per-tenant BudgetTracker registry without a tenant identity model would either invent an identity (wrong layer) or take an opaque key from the caller (no benefit over the current per-workflow tracker). | Ship a tenant_id parameter now: locks the API before the tenancy model is decided. |
| D-BUDGET-5 | 2026-05-18 | **No public API pin change for the new method.** `acknowledge_budget_exceeded` is added to the already-public `DurableWorkflow` class. The Tier 2.2 pin (`test_public_api_stability.py`) snapshots `__all__` set + load-bearing callables (`EncryptedCheckpointStore.__init__`, `.seal`, `.unseal`, `chain_migrations`) + dataclass fields. DurableWorkflow methods are not individually pinned — the pin already catches the load-bearing failure (kwarg drift on constructors and seal/unseal). | Pinning every method on every class would balloon the test file with no proportionate benefit. Method additions are minor bumps; the semver policy already covers them. | Pin every DurableWorkflow method now: high maintenance cost, low marginal coverage. Optional follow-up if methods become a recurring drift source. |
